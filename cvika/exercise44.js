/* Function Expression Exercise
Udemy's coding exercise platform does NOT support the ** operator
You'll need to multiply a number by itself or use the Math.pow() method */

const square = function (x) {
    return Math.pow(x, 2);
}

//Ako by to vyzeralo, keby udemy nebolo z roku 2016
const naDruhu = function (y) {
    return y ** 2;
}