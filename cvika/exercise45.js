/* Methods Exercise
square.area(10) //100
square.perimeter(10) //40 */

const square = {
    area(side) {
        return side * side;
    },
    perimeter(side) {
        return side * 4
    }
}